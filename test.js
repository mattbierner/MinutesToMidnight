const m2m = require('./index');

console.log(`getSeconds — ${m2m.getSeconds()}`)
console.log(`getMinutes — ${m2m.getMinutes()}`)
console.log(`getFractionalMinutes — ${m2m.getFractionalMinutes()}`)
console.log(`getTimeToMidnight — ${m2m.getTimeToMidnight()}`)
console.log(`getTime — ${m2m.getTime()}`)